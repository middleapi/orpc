import { defineComponents } from 'blume'

// Statically parsed by Blume (never executed) — keep values literal.
export default defineComponents({
  mdx: {
    SponsorSlot: './sponsor-ads/SponsorSlot.astro',
  },
})
