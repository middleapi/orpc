services:
  postgres:
    image: postgres:17-alpine
    container_name: nuxt-orpc-playground-postgres
    ports:
      - '5433:5432'
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: nuxt_orpc_playground
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres -d nuxt_orpc_playground']
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres-data:
